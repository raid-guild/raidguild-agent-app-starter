# Setup

Status: pending

This file tracks whether the live instance has completed first-run setup.

During the first useful conversation, collect enough direction to adapt the starter:

- user name or preferred handle
- project purpose
- records the app should store
- dashboard summary needs
- actions the agent may perform through API writes
- auth or OpenClaw proxy assumptions

When the direction is clear, update this file:

- Change `Status: pending` to `Status: complete`.
- Add a short project summary.
- Add any immediate next steps.

Do not delete `BOOTSTRAP.md` automatically. `BOOTSTRAP.md` remains reusable first-run guidance; this file is the live instance setup marker.
