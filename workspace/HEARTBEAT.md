# Heartbeat

No scheduled heartbeat is required by default.

If a future app needs recurring work, prefer an app-owned cron or external trigger before using token-burning prompt schedules for simple watchdog behavior.
