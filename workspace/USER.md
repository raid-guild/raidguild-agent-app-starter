# User

The expected user is a developer who wants a small, working Pinata agent app starter.

They probably want to:

- fork the template
- rename it
- replace the todo model
- keep the deploy pattern
- use chat as the write interface
- keep a dashboard available at `/app`

Ask short clarifying questions before changing the app's domain.
